import React from 'react'

const ItemDetail = ({id}) => {
    return (
        <section className="">
            <h3 className="text-light">Item con id {id}</h3>
        </section>
    )
}

export default ItemDetail
